// Export history store (persisted)
export { useHistoryStore } from "./historyStore";
export type { HistoryItem } from "./historyStore";

// Export popup store (non-persisted)
export { usePopupStore } from "./popupStore";